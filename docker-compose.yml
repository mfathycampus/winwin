services:
  postgres:
    image: postgres:16-alpine
    container_name: winwin_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: winwin
      POSTGRES_PASSWORD: winwin_secret
      POSTGRES_DB: winwin_db
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: winwin_redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes

  mailhog:
    image: mailhog/mailhog
    container_name: winwin_mailhog
    ports:
      - '1025:1025'
      - '8025:8025'

volumes:
  postgres_data:
  redis_data:
